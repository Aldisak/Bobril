import * as b from "bobril";

export const sideBarHeaderStyle = b.styleDef({
    marginLeft:"8px",
    marginRight:"8px",
    marginTop:"8px",
    height:"48px",
});

export const sideBarUserInfo = b.styleDef({
    display: "flex",
    alignItems: "center",
    paddingBottom: "12px"
})

export const sideBarItemsStyles = b.styleDef({
    marginTop: "160px"
})

export const appBarLeftContentStyle = b.styleDef({
    display: "flex",
    alignItems:"center",
    paddingLeft:"24px",
    paddingTop:"8px"
})

export const chatStyle = b.styleDef({
    marginTop: "32px"
})