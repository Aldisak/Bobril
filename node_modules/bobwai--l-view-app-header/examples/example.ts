import * as b from "bobril";
import * as Example from "bobwai--example";
import * as L_App from "bobwai--l-app";
import * as LViewAppHeader from "../src/lib";

const sampleContent = b.styledDiv("Header - should have browser scrollbar when to narrow", b.styleDef({ background: "lime" }));

Example.init({}, () => {
    return [
        L_App.create({
            children: LViewAppHeader.create({ children: sampleContent }),
        }),
    ];
});
