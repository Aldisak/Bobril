import * as b from "bobril";

export const textDirectionRtlStyle = b.styleDef({
    direction: "rtl",
    unicodeBidi: "embed"
});

export const rtlTextAlignLeftStyle = b.styleDef({
    textAlign: "left"
});

export const rtlTextAlignCenterStyle = b.styleDef({
    textAlign: "center"
});

export const rtlTextAlignRightStyle = b.styleDef({
    textAlign: "right"
});
