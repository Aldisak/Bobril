import * as flux from "bobflux";
import { setValidations } from "./setValidations";

export function resetValidations<TState>(cursor: flux.ICursor<TState>) {
    setValidations({
        errorInfo: null,
        validationRules: null,
        cursor,
    });
}
