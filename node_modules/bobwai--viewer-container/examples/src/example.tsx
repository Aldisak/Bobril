import * as b from "bobril";
import { ViewerContainer } from "../../src/lib";

export function getExampleInTsx(): b.IBobrilNode {
    return (
        <ViewerContainer
            header={"TSX TEST"}
            highlightedDescription={"TSX"}
            isTest
            description={"There is children and content property, if the both properties are set, content property will be used"}
        >
            Viewer container is ready for TSX!
        </ViewerContainer>
    );
}
