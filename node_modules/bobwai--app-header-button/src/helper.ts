import { IData, Type } from "./data";

export function isNavigationTypeButton(data: IData): boolean {
    return data.type === undefined || data.type === Type.Navigation;
}

export function isActionTypeButton(data: IData): boolean {
    return data.type === Type.Action;
}
