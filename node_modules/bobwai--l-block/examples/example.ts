import * as L_Block from "../src/lib";
import * as Container from "bobwai--viewer-container";
import * as Example from "bobwai--example";

Example.init({}, () => {
    return [
        Container.create({
            header: "l-block with background and padding",
            content: L_Block.create({
                background: "yellow",
                paddingLeft: 50,
                paddingTop: 20,
                paddingRight: 50,
                paddingBottom: 20,
                children: L_Block.create({
                    children: "inner l-block",
                    background: "orange",
                }),
            }),
        }),
    ];
});
