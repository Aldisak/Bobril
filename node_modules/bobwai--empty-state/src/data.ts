import * as b from "bobril";

export enum Size {
    Large,
    Medium,
    Small,
}

export enum IconType {
    EmptyApps,
    EmptyFolder,
    EmptyWorkflow,
    Error4xx,
    Error5xx,
    ErrorUser,
    NotConfigured,
    NotFound,
    Select,
    Success,
}

export interface IData {
    message: string | string[];
    iconType?: IconType;
    beforeLinkContent?: b.IBobrilChildren;
    linkContent?: b.IBobrilChildren;
    afterLinkContent?: b.IBobrilChildren;
    size?: Size;
    buttons?: b.IBobrilChild[];
}
