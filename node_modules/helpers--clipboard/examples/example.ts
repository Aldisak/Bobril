import * as b from "bobril";
import * as Container from "bobwai--viewer-container";

b.init(() => {
    return [
        Container.create({
            header: "No example",
            content: ["This helper allows you to copy string to clipboard with hidden div. See bobwai--context-menu demo. "]
        })
    ];
});
