import * as LApp from "bobwai--l-app";
import * as LBlock from "bobwai--l-block";
import * as Example from "bobwai--example";
import * as layoutContentWithFooter from "../src/lib";

Example.init({ preferredHeight: Example.PreferredHeight.Medium, debug: true }, () => {
    return [
        LApp.create({
            children: layoutContentWithFooter.create({
                bodyContent: LBlock.create({
                    background: "#6EDD1F",
                    paddingLeft: 0,
                    paddingRight: 0,
                    height: "100%",
                    children: "CONTENT PART"
                }),
                footerContent: LBlock.create({
                    background: "#A5E8FF",
                    paddingLeft: 0,
                    paddingRight: 0,
                    height: 50,
                    children: "FOOTER PART (footer height can be customized via customFooterHeight)"
                })
            })
        })
    ];
});
