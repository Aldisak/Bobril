import * as Container from "bobwai--viewer-container";
import * as Example from "bobwai--example";

Example.init({}, () => {
    return [
        Container.create({
            header: "helpers--web-content-accessibility",
            content: "Helper component with functions and structures for adding WCAG aria tags for automatic screen readers",
        }),
    ];
});
