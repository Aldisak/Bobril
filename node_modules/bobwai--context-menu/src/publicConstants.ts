import * as Color from "bobwai--color";

export let ItemPaddingLeft = 20;
export let ItemBackgroundColor = Color.White;
export let ItemHoverColor = Color.Grey200;

let propMinWidth: number = 160;
export function MinWidth(): number {
    return propMinWidth;
}
export function setMinWidth(val: number) {
    propMinWidth = val;
}
