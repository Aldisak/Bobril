import * as Container from "bobwai--viewer-container";
import * as Example from "bobwai--example";

Example.init({}, () => {
    return [
        Container.create({
            header: "bobwai--form-elements - it is only help component for sharing data types among form components, it has no example",
            content: "",
        }),
    ];
});
