// import * as b from 'bobril';
import * as Container from "bobwai--viewer-container";
import * as Example from "bobwai--example";

Example.init({}, () => {
    return [
        Container.create({
            header: "helpers--form-context",
            content: ["Helpers contains data structures which are propagated from bobwai form component into children components"],
        }),
    ];
});
