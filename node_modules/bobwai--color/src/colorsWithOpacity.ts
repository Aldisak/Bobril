import { getTransparentColor } from "./helper";
import { Basic } from "./lib";
import { Opacity } from "./opacity";

export function Basic_Opacity200(): string {
    return getTransparentColor(Basic(), Opacity.Opacity200);
}

export function Basic_Opacity300(): string {
    return getTransparentColor(Basic(), Opacity.Opacity300);
}
