import * as b from "bobril";
import { ICodeGeneratorAdditionalSettings } from "./data";
import { CodeType } from "./codeGenerator";
import { IImport } from "./importStrings";

export interface IControlPanelContext extends b.IBobrilCtx {
    isCollapsed: boolean;
    areAdvancedParametersVisible: boolean;
}

export interface ICodeGeneratorContext extends b.IBobrilCtx {
    isCodeVisible: boolean;
    imports: IImport[];
    codeText: string;
    selectedCodeType: CodeType;
    selectedPredefinedDataIndex?: number;
    propertiesFilter?: string;
}

export interface IInternalCodeGeneratorAdditionalSettings extends ICodeGeneratorAdditionalSettings {
    isIColor?: boolean;
}
