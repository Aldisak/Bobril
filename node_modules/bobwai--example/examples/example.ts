import * as example from "../src/lib";
import * as b from "bobril";

example.init({ preferredHeight: 200, debug: true }, () => {
    return [
        b.styledDiv("you could force options using these url params:"),
        b.styledDiv("- debug=0|1 to disable/enable debug mode"),
        b.styledDiv("- toolbar=0|1 to hide/show toolbar (colors & invalidate detection)"),
        b.styledDiv("- height=<number> to set height"),
    ];
});
